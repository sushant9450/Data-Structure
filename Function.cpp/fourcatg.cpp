#include <bits/stdc++.h>
using namespace std;
int sum(int x,int y){      // with return with parameter
    return (x+y);
}
int main(){
  int x,y,z;
  z=sum(2,3);
  cout<<z<<endl;
  return 0;
}
  
 /* void sum(int x,int y){       //with no return only parameter
       int z=(x+y);
      cout<<z<<endl;
  }
      int main(){
          int x,y;
          sum(2,3);
          return 0;
      }
      */
  /*int sum(){                 // with return no paramater
      int x,y;
      cin>>x>>y;
       return (x+y);
  }   
  int main(){              
      int z=sum();
    //cin>>x>>y;
      cout<<z<<endl;
      return 0;
  }     */
  
  /*void sum(){                   // no return no parameter
      int x,y;
      cin>>x>>y;
      int z=x+y;
      cout<<z<<endl;
  }
  int main(){
  sum();
  return 0;
  }  */
  
  
  

